# B社 FW_B01_PRIMARY (mock config)
hostname FW_B01_PRIMARY

# Interfaces (sanitized / demo)
interface ethernet1/1
  comment "to EDGE_ROUTER_B01"
  ip address 203.0.113.2/30
  zone untrust
interface ethernet1/2
  comment "to CORE_SW_B01 (inside)"
  ip address 10.20.0.1/24
  zone trust

# High Availability (demo)
ha group FW_B_HA_GROUP
  mode active-passive
  peer-ip 10.255.0.2
  heartbeat ethernet1/8

# Routing (demo)
virtual-router VR-DEFAULT
  static-route 0.0.0.0/0 next-hop 203.0.113.1

# Policies (minimal)
policy allow-trust-to-untrust
  from trust to untrust
  application any
  action allow

# Logging (demo)
log-setting default
  traffic yes
  threat yes
